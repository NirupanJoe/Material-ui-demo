import { createTheme } from '@material-ui/core';

const darkTheme = createTheme({
	palette: {
		primary: {
			main: '#9af634',
			contrastText: '#fff',
		},
		secondary: {
			main: '#787be2',
		},
	},
});

export default darkTheme;
